/*
 * Personal Messages
 *
 * This contains all the text for the Personal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Personal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Personal container!',
  },
});
