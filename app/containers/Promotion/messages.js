/*
 * Promotion Messages
 *
 * This contains all the text for the Promotion container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Promotion';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Promotion container!',
  },
});
