/*
 * SalonDetail Messages
 *
 * This contains all the text for the SalonDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SalonDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SalonDetail container!',
  },
});
